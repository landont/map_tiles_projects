# Simple Map Test - Shared Components Only
cmake_minimum_required(VERSION 3.5)

# This directory contains shared components only
message(STATUS "This directory contains shared components only")

# Dummy project to prevent cmake errors when run from root
project(simple_map_shared)
